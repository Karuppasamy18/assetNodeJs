doctype html
html
    head
        title Issue/Return List
        style.
            body {
                font-family: Arial, sans-serif;
                background-color: #f4f4f4;
                margin: 0;
                padding: 20px;
            }

            h1 {
                text-align: center;
                color: #333;
            }

            p {
                text-align: center;
                color: #666;
            }

            /* Table Styles */
            table {
                width: 80%;
                margin: 20px auto;
                border-collapse: collapse;
                background-color: #fff;
                box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            }

            th, td {
                padding: 10px;
                text-align: left;
                border-bottom: 1px solid #ddd;
            }

            th {
                background-color: #4CAF50;
                color: white;
            }

            tr:nth-child(even) {
                background-color: #f2f2f2;
            }

            tr:hover {
                background-color: #f1f1f1;
            }

            .btn {
                display: block;
                width: 150px;
                margin: 20px auto;
                padding: 10px;
                border: none;
                background-color: #4CAF50;
                color: white;
                font-size: 16px;
                cursor: pointer;
                border-radius: 5px;
                text-align: center;
                text-decoration: none;
            }

            .btn:hover {
                background-color: #45a049;
            }

            .delete-link {
                color: red;
                margin-left: 10px;
                cursor: pointer;
            }

            .delete-link:hover {
                text-decoration: underline;
            }

    body
        h1 Issue/Return List
        if issueIns.length === 0
            p Not found.
        else
            table
                thead
                    tr  
                        th SNO
                        th Type
                        th Emp Name
                        th Remarks
                        th action
                tbody
                    each issue, index in issueIns
                        tr
                            td #{index + 1} 
                            td #{issue.type}  
                            td #{issue.employee.firstName}
                            td #{issue.reason}                     
                            td  
                                a(href=`/issueDetails/${issue.id}`) Details
                                a(href=`/editissue/${issue.id}`) Edit
                                a(href=`#`, onclick=`deleteAsset('${issue.id}')`, class='delete-link') Delete

        button.btn(type='button' onclick="location.href='/issueCreate'") Create Issue/Return

    //- script.
    //-     function deleteAsset(assetId) {
    //-         if (confirm('Are you sure you want to delete this employee?')) {
    //-             fetch(`/deleteAsset/${assetId}`, {
    //-                 method: 'POST',  
    //-             })
    //-             .then(response => {
    //-                 if (response.ok) {
    //-                     location.reload();  
    //-                 } else {
    //-                     throw new Error('Failed to delete employee');
    //-                 }
    //-             })
    //-             .catch(error => {
    //-                 console.error('Error deleting employee:', error);
    //-                     alert('Failed to delete employee. Please try again.');
    //-             });
    //-         }
    //-     }
